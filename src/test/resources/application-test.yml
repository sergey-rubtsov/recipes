spring:
  application:
    name: recipes
  jpa:
    show-sql: true
    properties:
      format_sql: true
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test

server:
  port: 8080
